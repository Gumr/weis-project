<view class="i-class i-load-more {{ loading ? '' : 'i-load-more-line' }}">
  <view class="i-load-more-loading" wx:if="{{ loading }}"></view>
  <view class="i-load-more-tip">
    <view wx:if="{{ loading }}">{{ loadingTip }}</view>
    <view wx:else>{{tip}}</view>
  </view>
</view>
